#include <stdio.h>
main()
{
	int d1, d2, d3, sum;
	float ave;
	printf("®”‚ğ‚RŒÂ“ü—Í : ");
	scanf("%d%d%d", &d1, &d2, &d3);
	sum = d1 + d2 + d3;
	printf("‡Œv=%d", sum);
	ave = sum / 3;
	printf("•½‹Ï=%.2f\n", ave);

}